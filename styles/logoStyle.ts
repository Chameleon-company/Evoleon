// Importing StyleSheet from react-native
import { StyleSheet } from 'react-native';

// Creating and exporting styles
export default StyleSheet.create({
    // Style for the front page logo
    frontPageLogo: {
        width: 200, // setting the width of the logo
        height: 200, // setting the height of the logo
        marginBottom: 125, // setting the bottom margin of the logo
    }
});
